// Jangan hapus baris import ini -- ini yang membuat #bp[...] dan helper
// lain dari template.typ bisa dipakai di file ini.
#import "../template.typ": *

#align(center)[#text(size: 20pt, weight: "bold")[ABSTRACT]]
#v(0.5cm)

#par(first-line-indent: 0cm)[#emph[Abstract ditulis dalam bahasa Inggris, sebagai terjemahan dari bagian Abstrak sebelumnya, dan ditulis menggunakan format huruf italic (miring).]]

#gap

#bp[#emph[Keywords : sebutkan 5 kata kunci.]]
